package com.lucasapds.finance.enums;

public enum LaunchType {

	REVENUE,
	EXPENSE;

	/*
	 * private int code;
	 * 
	 * private LaunchType(int code) { this.code = code; }
	 * 
	 * public int getCode() { return code; }
	 * 
	 * public static LaunchType valueOf(int code) { for (LaunchType value :
	 * LaunchType.values()) { if (value.getCode() == code) { return value; } }
	 * 
	 * throw new IllegalArgumentException("Invalid LaunchType code"); }
	 */
}
